<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
     http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet author="claude" id="patient-merge-menu-localization-1">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM clinlims.localization WHERE description = 'banner.menu.patient.merge'
            </sqlCheck>
        </preConditions>
        <comment>Adds localization entries for Patient Merge menu item</comment>
        <sql>
            INSERT INTO clinlims.localization (id, description, english, french, lastupdated)
            VALUES
            (nextval('localization_seq'), 'banner.menu.patient.merge', 'Merge Patient', 'Fusionner Patient', CURRENT_TIMESTAMP),
            (nextval('localization_seq'), 'banner.menu.patient.merge.tooltip', 'Merge duplicate patient records', 'Fusionner les dossiers patients en double', CURRENT_TIMESTAMP);
        </sql>
    </changeSet>

</databaseChangeLog>
