package org.openelisglobal.alert.valueholder;

public enum AlertSeverity {
    /**
     * Warning-level alerts (non-critical, requires attention)
     */
    WARNING,

    /**
     * Critical-level alerts (requires immediate attention and action)
     */
    CRITICAL
}
